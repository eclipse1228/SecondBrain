```java 
ArrayList<String> list = new ArrayList<>();
```
배열 크기 동적으로 변경하면서 사용가능

```java
ArrayList<Book> list  = new ArrayList<>();
```

^696436

가능합니다. 즉 객체도 받을 수 있어요. 
```java
List.add("철수")
```
<h3>list.size() , list.get()</h3>

```java
list.size() 하면 크기 나옵니다.
```

```java
list.get(index) 해서 가져올 수 있습니다.! 즉 add와 get을 이용합니다. set은 변경하
```
